// String, number, undefined, null, boolean, symbol

const nome = 'ana'; // String
const nome1 = "ana"; // string
const nome2 = `ana`; // string
const num1 = 10; // number
const num2 = 10.25; // number
let nomeAluno; // undefined: não aponta para local nenhum na memória
const sobrenomeAluno = null; // null: não aponta para local nenhum na memória
const aprovado = false; // Boolean = true, false (lógico)

console.log(typeof nome, nome);

/**
 *  String letras, nomes
 *  number numeros
 *  Boolean true, false
 *  Float numero com casas decimais, com virgula ou ponto.
 * 
 */